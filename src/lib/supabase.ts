/**
 * ATENÇÃO: Este arquivo foi substituído pela pasta /supabase/
 * Use:
 *   - @/lib/supabase/browser  → em componentes "use client"
 *   - @/lib/supabase/server   → em Server Components e API Routes
 *
 * Este re-export existe para não quebrar importações legadas.
 */
export { createClient } from './supabase/browser';
